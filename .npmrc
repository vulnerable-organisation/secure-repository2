# Use private registry for @example-business scoped packages
@example-business:registry=https://npm.example-business.com/
//npm.example-business.com/:_authToken=${NPM_TOKEN}
always-auth=true

# Default registry fallback (still public for non-scoped packages)
registry=https://registry.npmjs.org/
progress=false
